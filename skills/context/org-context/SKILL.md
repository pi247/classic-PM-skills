---
name: org-context
description: >
  Capture or revise organisation-level shared context (governance, vocabulary, standards)
in ORG_CONTEXT.md. Use when setting up an organisation, aligning terminology, or before
starting projects that inherit org rules.
license: MIT
metadata:
  classic-pm.pack: classic-pm-skills
  classic-pm.version: "1.0"
  classic-pm.artifact: none
  classic-pm.level: org
---

# Org context

Maintain portable `ORG_CONTEXT.md` (see `docs/artifact-contract.md` and `examples/context/ORG_CONTEXT.md`).

## Steps

1. Read existing file and any setup paths in `.classic-pm/paths.md`.
2. Fill gaps with the user; prefer `/stress-test` (docs mode) when assumptions conflict.
3. Keep strategy/OKR detail as **pointers**, not duplicated rows.
4. In Pi247, dual-write to reserved hub page slug `org-context` when that bridge exists.
